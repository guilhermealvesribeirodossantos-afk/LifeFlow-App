<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission
        android:name="android.permission.INTERNET" />

    <application
        android:theme="@style/Theme.LifeFlow"
        android:label="LifeFlow"
        android:icon="@drawable/lifeflow_icon"
        android:roundIcon="@drawable/lifeflow_icon"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false">

        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Ícone do LifeFlow -->
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Domínio oficial do LifeFlow -->
            <intent-filter android:autoVerify="true">

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="guilhermealvesribeirodossantos-afk.github.io" />

            </intent-filter>

        </activity>

    </application>

</manifest>
